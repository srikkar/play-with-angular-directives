import { ConsoleLogDirective } from './console-log.directive';

describe('ConsoleLogDirective', () => {
  it('should create an instance', () => {
    const directive = new ConsoleLogDirective();
    expect(directive).toBeTruthy();
  });
});
